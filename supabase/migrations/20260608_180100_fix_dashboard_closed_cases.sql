begin;

-- Esta migración quedó consolidada en la versión final
-- `20260608_181000_fix_dashboard_snake_case_keys.sql`.
-- Se conserva como no-op para no romper el orden histórico local
-- y para evitar reintroducir un contrato intermedio inválido.

commit;
