import { DashboardHome } from "../../dashboard/pages/DashboardHome";

export function HomePage() {
  // El antiguo HomePage (monolítico) ha sido reemplazado por la arquitectura 
  // modular de Centro de Mando (DashboardHome).
  return <DashboardHome />;
}
